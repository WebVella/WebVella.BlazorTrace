@namespace WebVella.BlazorTrace
@using WebVella.BlazorTrace.Utility

@if (Data is not null && Data.Request is not null && Data.Request!.IsSignalLogMenu)
{
	<WvBlazorTraceConsole Items="Data.SignalLog" />
}
@code {
	[Parameter] public WvTraceModalData? Data { get; set; } = null;
}